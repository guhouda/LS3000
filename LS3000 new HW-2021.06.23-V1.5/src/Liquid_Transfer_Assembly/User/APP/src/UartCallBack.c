#include "bsp.h"
#include "BackWork.h"

// 宏定义
#define MAX_MSG_NUM			128

// 串口通信回调函数
static void UartCallBack_SC(COMM_PARAM *pFrame) 
{
	
}

// Uart1通信回调处理
void Uart1CallBack_SC(COMM_PARAM *pFrame)
{
    UartCallBack_SC(pFrame);
}

// Uart2通信回调处理
void Uart2CallBack_SC(COMM_PARAM *pFrame)
{
    UartCallBack_SC(pFrame);
}

// Uart3通信回调处理
void Uart3CallBack_SC(COMM_PARAM *pFrame)
{
    UartCallBack_SC(pFrame);
}

// Uart4通信回调处理
void Uart4CallBack_SC(COMM_PARAM *pFrame)
{
    UartCallBack_SC(pFrame) ;
}

// Uart5通信回调处理
void Uart5CallBack_SC(COMM_PARAM *pFrame)
{
    UartCallBack_SC(pFrame);
}

// Uart6通信回调处理
void Uart6CallBack_SC(COMM_PARAM *pFrame)
{
    UartCallBack_SC(pFrame);
}

// Uart7通信回调处理
void Uart7CallBack_SC(COMM_PARAM *pFrame)
{
    UartCallBack_SC(pFrame);
}

// Uart8通信回调处理
void Uart8CallBack_SC(COMM_PARAM *pFrame)
{
    UartCallBack_SC(pFrame) ;
}
